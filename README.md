# Credit-Card-Splitter-
An Excel macro-enabled workbook (.xlsm) that automatically splits a bulk credit card transaction export into individual per-person statement sheets, then generates Outlook email drafts with PDF attachments — one per statement recipient.
